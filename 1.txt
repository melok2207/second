#include <stdio.h>

int main() {
    double t1, t2, t3;

    if (scanf("%lf %lf %lf", &t1, &t2, &t3) != 3) {
        return 1;
    }

    double total_time = 1 / (1/t1 + 1/t2 + 1/t3);

    printf("%.2f\n", total_time);

    return 0;
}
